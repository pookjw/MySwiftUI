public import os

@available(macOS 10.15, iOS 13.0, macCatalyst 13.0, tvOS 26.0, *)
public struct __REOSLog {
    @safe public static nonisolated(unsafe) let UpdateLoop: OSLog = {
        assertUnimplemented()
    }()
    
    @safe public static nonisolated(unsafe) let ARQL: OSLog = {
        assertUnimplemented()
    }()
    
    @safe public static nonisolated(unsafe) let RealityComposer: OSLog = {
        assertUnimplemented()
    }()
}
